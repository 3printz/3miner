elastic {
  cluster = "zchain"
  cluster = ${?ELASTIC_CLUSTER}
  indexTransaction = "transaction"
  indexTransaction = ${?ELASTIC_INDEX_TRANSACTION}
  indexBlocks = "blocks"
  indexBlocks = ${?ELASTIC_INDEX_BLOCKS}
  host = "10.4.1.15"
  host = ${?ELASTIC_HOST}
  port = 9300
  port = ${?ELASTIC_PORT}
}

